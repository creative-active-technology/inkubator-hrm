<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:batch="http://www.springframework.org/schema/batch" 
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.2.xsd">

	<!-- stored job-meta in database -->
	<bean id="jobRepository" class="org.springframework.batch.core.repository.support.JobRepositoryFactoryBean">
		<property name="dataSource" ref="c3p0DataSource-1" />
		<property name="transactionManager" ref="transactionManager" />
	</bean>

	<!-- calling this to running job -->
	<bean id="jobLauncher" class="org.springframework.batch.core.launch.support.SimpleJobLauncher">
		<property name="jobRepository" ref="jobRepository" />
	</bean>
	<bean id="jobLauncherAsync" class="org.springframework.batch.core.launch.support.SimpleJobLauncher">
		<property name="jobRepository" ref="jobRepository" />
		<!-- To make execution asynchronously. For further detail, please refer to this page http://docs.spring.io/spring-batch/trunk/reference/htmlsingle/#configuringJobLauncher -->
		<property name="taskExecutor">
			<bean class="org.springframework.core.task.SimpleAsyncTaskExecutor" />
		</property>
	</bean>
	
	<!-- ALL batch jobs -->
	<batch:job id="jobPaySalaryUpload">
		<batch:step id="stepPaySalaryUpload_1">
			<batch:tasklet>
				<batch:chunk reader="customPaySalaryUploadItemReader" writer="customPaySalaryUploadItemWriter"
					commit-interval="10">
				</batch:chunk>
			</batch:tasklet>
		</batch:step>
	</batch:job>	
	<batch:job id="jobPayEmployeeCalculation">
		<batch:step id="stepPayEmployeeCalculation_1">
			<batch:tasklet>
				<batch:chunk reader="customPayEmployeeCalculationItemReader" writer="customPayEmployeeCalculationItemWriter"
					commit-interval="10">
				</batch:chunk>
			</batch:tasklet>
		</batch:step>
	</batch:job>
	
	
	<!-- reader and writer for pay_salary_upload -->	
	<bean id="customPaySalaryUploadItemReader" class="com.inkubator.hrm.batch.PaySalaryUploadReader" scope="step">
	    <constructor-arg name="filePath"  value="#{jobParameters['input.file.path']}"></constructor-arg>
	    <property name="createdBy" value="#{jobParameters['createdBy']}"/>
	    <property name="paySalaryComponentId" value="#{jobParameters['paySalaryComponentId']}"/>
	</bean>	
	<bean id="customPaySalaryUploadItemWriter" class="org.springframework.batch.item.adapter.ItemWriterAdapter">
	    <property name="targetObject" ref="payTempUploadDataService" />
	    <property name="targetMethod" value="executeBatchFileUpload" />
	</bean>
	
	
	<!-- reader and writer for pay_employee_calculation -->
	<bean id="customPayEmployeeCalculationItemReader" class="com.inkubator.hrm.batch.PayEmployeeCalculationReader" scope="step">
		<constructor-arg name="payTempKalkulasiService" ref="payTempKalkulasiService"/>
	</bean>	
	<bean id="customPayEmployeeCalculationItemWriter" class="org.springframework.batch.item.database.JdbcBatchItemWriter">
		<property name="dataSource" ref="c3p0DataSource-1" />
		<property name="sql">
			<value>
            <![CDATA[        
            	insert into pay_temp_kalkulasi(id,pay_salary_component_id,emp_data_id,nominal,factor,created_on,created_by,version) values (:id,:paySalaryComponent.id,:empData.id,:nominal,:factor,:createdOn,:createdBy,0)
            ]]>
			</value>
		</property>
		<!-- It will take care matching between object property and sql name parameter -->
		<property name="itemSqlParameterSourceProvider">
			<bean class="org.springframework.batch.item.database.BeanPropertyItemSqlParameterSourceProvider" />
		</property>
	</bean>
	
	
</beans>